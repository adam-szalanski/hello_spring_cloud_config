eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    
    
microservice:
  server-microservice:
    endpoints:
      endpoint:
        uri: http://SERVER-MICROSERVICE/owner
